# BEHAVIOR — `TRCC.LCD/FormLCD.cs`, the shared-memory ultrawide host

<!-- audit-state: origin=2.1.6.0 addresses=2.1.6.0 known-bad=none -->
> **Audited against TRCC 2.1.6.**
> Every method it documents is byte-identical in TRCC 2.1.6.
> [`AUDIT_INDEX.md`](AUDIT_INDEX.md#provenance)
<!-- /audit-state -->

`TRCC.LCD` is a namespace that did not exist in the release the rest of these
audits describe. `FormLCD.cs` is 5,082 lines of it — a host form parallel to the
`TRCC.CZTV/FormCZTV.cs` in [`BEHAVIOR_FORMCZTV.md`](BEHAVIOR_FORMCZTV.md), with
its own `FormCZTVInit` and its own theme I/O.

**It is not a second encoder.** `ImageToJpg`, `ImageTo565` and `GetEncoder` do
not exist in this file; it never builds a wire frame. What it has instead is the
shared-memory pair — `InitMemorySize`, `ReadShareMemory`, `WriteShareMemory`,
`SetConfigurationToMemory`, `GetConfigurationToMemory` — and it launches the
helper executable `LCD\TRCCLCDAPP.exe`, which owns the wire. This is the second
of the two discovery worlds `AUDIT_DISCOVERY.md` describes: in-process HID on
one side, an external process fed through a memory-mapped file on the other.

The device it serves is a specific one. `FormCZTVInit` walks `Screen.AllScreens`
and counts only monitors whose bounds are exactly **2560×720 or 720×2560**,
taking the `myDeviceCount`-th of them for its geometry, and its remaining
branches key on **`fbl == 257`** — the shared-memory RGB LCD that
`AUDIT_DISCOVERY.md` records as ID `257` (`USB_ID1_1`), added by
`RGB_ADD_Device` rather than by HID enumeration. So this host drives an
ultrawide strip panel that presents to Windows as a monitor, not a panel we
build frames for.

**What that means for the port:** nothing in our render path is contradicted by
this file, which is the useful negative result — the encoder conclusions in
`AUDIT_LCD_PIPELINE.md` stand as the only ones. What it does describe is a
device class we do not support at all, and would reach through a completely
different seam if we ever did.

**Read this doc as a map, not as settled protocol.** It is the first pass over a
file nothing had read.

Companion: [`AUDIT_LCD_PIPELINE.md`](AUDIT_LCD_PIPELINE.md) owns the
compose→rotate→wire model recovered from `FormCZTV`; entries here that touch the
same ground cross-reference it rather than restating it.

Source policy applies: facts, never pasted source — see
[`AUDIT_INDEX.md`](AUDIT_INDEX.md#source-policy--facts-not-source-text).

## Methods
- `FormLCD` (FormLCD.cs:852) — constructor. Runs `InitializeComponent`, then caches the DPI factor into `sfblJP` via `GetScreenScalingFactor` (FormLCD.cs:663). Zeroes the flat-border colour to ARGB(0,0,0,0) on eight buttons (`buttonBDZT`, `buttonYDZT`, `buttonZTSZ`, `buttonYDMB`, `buttonDWXS`, `buttonHelp`, `buttonPower`, `buttonLDD`). Creates `formJP` (`FormScreenshot`) and `formGetColor` (`FormGetColor`, initialised with `sfblJP`) and hooks their callbacks to `UpDateFormScreenshot` / `UpDateFormGetColor`. Wires the four content panels to their handlers (`ucThemeLocal1`→`ThemeLocal`, `ucThemeWeb1`→`ThemeWeb`, `ucThemeSetting1`→`ThemeSetting`, `ucThemeMask1`→`ThemeMask`). Sets `GifDirectory` to the data-relative path `USBLCD\Theme` (FormLCD.cs:873). Builds the rotation selector `ucComboBoxA1` with `myCount = 4` and the exact entry list `0°`, `90°`, `180°`, `270°` (four trailing empty slots), backing image `P下拉框底图4X` (FormLCD.cs:874-877). Hooks `ucImageCut1`, `ucVideoCut1` and the brightness scroller `ucScrollB1`→`UpDateUCScrollB`. Calls `CheckDirectoryExist` then `FormCZTVLanguageSet`, allocates `themeArray`, and hides `formJP`, `formGetColor` and `ucThemeLocal1`. No branches.
- `FormCZTVInit` (FormLCD.cs:903) — [GOD] device attach entry point; parameters are `fbl` (resolution/panel id), `m`→`myDeviceMode` (default 1), `count`→`myDeviceCount` (device index, default 0), `ysl`→`myDeviceJpgYSL` and `myTempDeviceJpgYSL` (JPEG quality, default 95), `pm`→`myDevicePingMu` (default 1), `name`→`myDeviceName` (per-device filename suffix, default none), `pmSub` (default 0). Walks `Screen.AllScreens` and counts only monitors whose bounds are exactly 2560×720 or 720×2560; the `myDeviceCount`-th such monitor supplies `screenX`, `screenY`, `GIFSizeW`, `GIFSizeH` (FormLCD.cs:910-926). Appends `myDevicePingMu` then `myDeviceCount` to `shareMemoryName` (base `TRCCLCDAPP`) and calls `InitMemorySize`, which creates/opens a 3145728-byte memory-mapped file under that name and launches the helper executable `LCD\TRCCLCDAPP.exe` (FormLCD.cs:314-328). Then per-device setup: rotation read via `GetNowDeviceDirection(name)` (FormLCD.cs:1005), theme sub-directory chosen via `SetThemeInfo_ThemeML(pmSub, fbl)` (FormLCD.cs:1056), colour presets loaded from `USBLCD\Device\Color.dc` with `name` appended when present, `fileThemeDir` set to `USBLCD\Theme` + `ThemeML`, and `fileThemeVal` set to `fileThemeDir` + `Theme.dc` with `name` appended when present. Note the `pmSub` and `fbl` arguments are passed on but `SetThemeInfo_ThemeML` ignores both — it branches only on `is2560x720` and `themeDirection`. Key branches: `fbl == 257` -> sets `is2560x720` true and positions both crop overlays (`ucImageCut1.formLCDImageCut`, `ucVideoCut1.formLCDImageCut`) to `myX/myY/myW/myH` = `screenX/screenY/GIFSizeW/GIFSizeH`, Show-then-set-Bounds-then-Hide to force the rectangle; `fbl != 257` -> sets `is2560x720` true anyway, crop overlays left unpositioned (so the flag is unconditionally true on every path, FormLCD.cs:929-950); `name != null` -> colour file and theme-index file get the device-name suffix, else the bare names; `ReadFileTheme` throws -> swallowed, theme list left as-is; `Theme_Click_Event(false)` throws -> `SetConfigurationToMemory(0)` is called as the fallback (FormLCD.cs:986-993); `Form1.Language != 100` -> shows the cloud-background and cloud-mask buttons (`buttonYDZT`, `buttonYDMB`) and their panels, i.e. language 100 is an offline/hidden-cloud build. Always enables `buttonBDZT`/`buttonZTSZ`/`buttonYDZT`/`buttonYDMB`, propagates `is2560x720` into `ucImageCut1`, `ucThemeSetting1.ucTouPingXianShi1` and `ucVideoCut1` (as `is2560720`), shows `ucThemeLocal1`, resets its buttons and refreshes `ucThemeSetting1.ucXiTongXianShiAdd1`. EXTERNAL: `Form1.AddDataFilePath` resolves the data root; `Theme_Click` / `Theme_Click_Event` live later in this file, outside the read range.
- `FormCZTVLanguageSet` (FormLCD.cs:679) — [COPY-PASTE] ten near-identical blocks that swap the same set of background images for a language-suffixed resource variant; should collapse to a suffix lookup table. Assigns, per language: the form background, the `BackgroundImage` **and** the `imageBk` field of `ucThemeLocal1` / `ucThemeMask1` / `ucThemeWeb1` (both must be set — `imageBk` is the panel's redraw source), and the backgrounds of seven `ucThemeSetting1` children (`ucBeiJingXianShi1`, `ucMengBanXianShi1`, `ucTouPingXianShi1`, `ucShiPingBoFangQi1`, `ucXiTongXianShi1`, `ucXiTongXianShiColor1`, `ucShortcut1`) — 14 assignments per branch. Key branches, all on `Form1.Language`: `1` -> unsuffixed resources (`F0副屏`, `P0本地主题`, `p0云端主题`, `p0云端背景`, `P01背景显示`, `P01布局蒙板`, `P01投屏显示xy`, `P01播放器`, `P01内容`, `P01参数面板`, `P01快捷方式`); `2` -> suffix `tc`; `3` -> suffix `d`; `4` -> suffix `e`; `5` -> suffix `f`; `6` -> suffix `p`; `7` -> suffix `r`; `8` -> suffix `x`; `9` -> suffix `h`, and this branch alone uses `P0CZTVh` for the form background instead of a `F0副屏` variant **and omits the `ucShortcut1` assignment entirely** (13 assignments, FormLCD.cs:817-832) — a real asymmetry, language 9 keeps whatever shortcut artwork was last set; any other value -> suffix `en` (the default/English path).
- `FormCZTVRemove` (FormLCD.cs:889) — device detach/teardown. Stops the timer gate (`isToTimer` false), snapshots the JPEG quality by copying `myDeviceJpgYSL` into `myTempDeviceJpgYSL`, empties `themeArray`, calls the three panel teardowns (`UCThemeLocalRemove`, `UCThemeMaskRemove`, `UCThemeWebRemove`), disposes those three panels as components, then `CloseShareMemory`. Note it does not reset `shareMemoryName` (which `FormCZTVInit` appends to) nor `lcdAppName` (which `InitMemorySize` rewrites), so a detach→attach cycle on the same form accumulates suffixes. No branches.
- `CheckDirectoryExist` (FormLCD.cs:671) — ensures the theme root `GifDirectory` (set in the constructor to the data-relative `USBLCD\Theme`) exists. Key branches: `directory missing -> Directory.CreateDirectory(GifDirectory)`; otherwise nothing. Only the root is created — the per-orientation subdirectories `2560720\` / `7202560\` are not.
- `ReadFileTheme` (FormLCD.cs:1081) — rebuilds the local theme list from disk. Clears `themeArray`, enumerates the subdirectories of `fileThemeDir` (= `USBLCD\Theme` + `ThemeML`), and orders them by: entries named in the priority list `Theme1`, `Theme2`, `Theme3`, `Theme4`, `Theme5` first (descending on membership), then by index within that list, then alphabetically by name — so the five shipped themes always lead, in numeric order, and user themes follow alphabetically (FormLCD.cs:1086-1089). Adds each surviving directory's `Name` to `themeArray`. Finishes by calling `ReadFileThemeSub(bl)`. Key branches: `directory contains >= 2 files -> added to themeArray`; `directory contains fewer than 2 files -> Directory.Delete(..., recursive: true)`, i.e. loading the theme list silently and permanently destroys any theme folder holding 0 or 1 file (FormLCD.cs:1092-1099); `bl == false` -> `ReadFileThemeSub` returns immediately, so the directory scan and the deletion still happen but the persisted `Theme.dc` selection is not re-read. Porting caveat: the file-count probe concatenates `fileThemeDir` with the `DirectoryInfo` object (its string form), whereas the list stores `.Name` — the two are not built the same way.
- `FontToBytes` (FormLCD.cs:376) — serialises a font to the blob embedded in shared-memory slot 1 (see `SetConfigurationToMemory`, FormLCD.cs:590). Field order, written through a UTF-8 `BinaryWriter`: family name as a .NET length-prefixed string (7-bit-encoded varint byte count, then UTF-8 bytes), size as a 4-byte little-endian float, `FontStyle` as int32, `GraphicsUnit` as int32, `GdiCharSet` as a single byte. Key branches: `font == null -> zero-length array`; `font.Name == null -> empty string written` (the varint length prefix is still emitted). Total blob length is stored as a 16-bit little-endian count at element offset +18 by the caller, so a font blob must stay under 65536 bytes.
- `BytesToFont` (FormLCD.cs:397) — inverse of `FontToBytes`; reads name (varint-length-prefixed UTF-8 string), size (float32), `FontStyle` (int32), `GraphicsUnit` (int32), `GdiCharSet` (byte) in that order and constructs the font from all five. Key branches: `data == null or data.Length == 0 -> SystemFonts.DefaultFont`; `any read or construction throws -> SystemFonts.DefaultFont` (so a truncated or foreign-endian blob degrades silently to the system default rather than failing loudly).
- `ClearMemoryMy` (FormLCD.cs:1196) — public static, takes a `memory` threshold defaulting to 20, and has a completely empty body: it does nothing at all. Dead API kept for call-site compatibility; a port should not implement it. No branches.
- `ClearMemorySelf` (FormLCD.cs:1200) — public static, takes a `memory` threshold defaulting to 1000, empty body, does nothing. Same dead-API situation as `ClearMemoryMy`; note the two differ only in their default threshold, and neither uses it. No branches.
- `ClearMemoryAll` (FormLCD.cs:1204) — the one memory routine that is not empty. Forces `GC.Collect` then `GC.WaitForPendingFinalizers`, then enumerates **every process on the machine** via `Process.GetProcesses()` and calls the psapi `EmptyWorkingSet` on each one's handle — not just this app's. Key branches: `EmptyWorkingSet or handle access throws for a process -> caught per-process and ignored, loop continues` (which is the normal case for processes the user cannot open). Has no Linux equivalent and no functional purpose for a port; it is a Windows working-set-trimming hack.
- `CloseShareMemory` (FormLCD.cs:346) — disposes the `MemoryMappedFile` handle, then scans every running process and kills any whose `ProcessName` contains `lcdAppName`. Key branches: `process.ProcessName contains lcdAppName -> process.Kill()`; `any exception during enumeration or kill -> swallowed, method returns`. Load-bearing defect for a porter: `InitMemorySize` (FormLCD.cs:319) overwrites `lcdAppName` with `LCD\TRCCLCDAPP.exe` before this ever runs, while `Process.ProcessName` yields a bare name with no directory and no extension — the `Contains` test can therefore never match after initialisation, so the spawned helper process is never actually terminated and survives the form. Compounding it, `InitMemorySize` re-prefixes the same field on every call.
- `EmptyWorkingSet` (FormLCD.cs:1194) — not a managed method: a private static P/Invoke declaration importing `EmptyWorkingSet` from `psapi.dll`, taking a process handle and returning an int. Its only caller is `ClearMemoryAll` (FormLCD.cs:1214). Declared alongside `SetProcessWorkingSetSize` from `kernel32.dll` (FormLCD.cs:1191), which is declared but never called anywhere in the read range. Behaviour is the Win32 API's; no managed body exists to describe. No branches.
- `ButtonNewMode` (FormLCD.cs:1294) — the single nav-tab switcher for the four right-hand panels. It first repaints all four tab buttons with their *unselected* resource art (`buttonBDZT`←`P本地主题`, `buttonYDZT`←`P云端背景`, `buttonZTSZ`←`P主题设置`, `buttonYDMB`←`p云端主题`), then selects one. Each arm swaps that one button to its `…a` (active) resource, shows exactly one panel and hides the other three. Note the two distinct cloud trees: `buttonYDZT`/`ucThemeWeb1` = cloud **backgrounds**, `buttonYDMB`/`ucThemeMask1` = cloud **masks**. Callers set `myModeUC` then pass it in: `buttonBDZT_Click` (1333)→1, `buttonYDZT_Click` (1339)→2, `buttonZTSZ_Click` (1349)→3, `buttonYDMB_Click` (1359)→4; the three cloud/settings tabs first cancel a running slideshow via `ucThemeLocal1.buttonLunbo_Set(false)` when `ucThemeLocal1.isLunbo` (1341, 1351, 1361). [COPY-PASTE] — four bodies identical apart from (button, active-resource, panel); collapses to one lookup row per tab. Key branches: `mode==1 -> P本地主题a + show ucThemeLocal1, hide ucThemeWeb1/ucThemeMask1/ucThemeSetting1`; `mode==2 -> P云端背景a + show ucThemeWeb1, hide the other three`; `mode==3 -> P主题设置a + show ucThemeSetting1, hide the other three`; `mode==4 -> P云端主题a + show ucThemeMask1, hide the other three`; `mode not in 1..4 -> no default arm, so all four buttons stay on unselected art and no panel is shown or hidden`.
- `GetSystemInfo` (FormLCD.cs:1627) — [GOD] the per-tick housekeeping pump; called from `Timer_event` (1713) and also once from line 3344. Four unrelated responsibilities in one body: colour-picker refresh, a 64-tick heavy block (system-info repaint + helper-process watchdog + game-mode CPU hysteresis + shared-memory config exchange), and an unrelated `shanPingTimer` countdown. Counters: `InfoCount` increments every call and wraps at 64; the colour picker fires on every 3rd tick. The 64-tick block repaints `ucThemeSetting1.ucXiTongXianShiAdd1` and calls `ucThemeSetting1.ucXiTongXianShi1.UCXiTongXianShiTimer()` (EXTERNAL: UCXiTongXianShi), then runs the watchdog `FupingJiance` (1593), which scans running processes for the exact name `TRCCLCDAPP` and, when absent, starts `Form1.AddDataFilePath(lcdAppName)` and reports not-running. Game-mode CPU load is read as **text** out of `Form1.ucSystemInfoOptions1.UCSystemInfoOptionsOneList[0].label2`, with `%` stripped and `Convert.ToInt32` applied; a parse failure is swallowed and leaves the value 0. The hysteresis counter `myCpuCount` is clamped to 0..10 against threshold `ucThemeLocal1.myCPU`, so 10 consecutive over-threshold ticks are needed to raise `myCpuHigh` and 10 under-threshold ticks to clear it. Key branches: `InfoCount % 3 == 0 -> formGetColor.MyTimer()`; `InfoCount < 64 (after increment) -> skip the whole heavy block`; `InfoCount >= 64 -> reset InfoCount to 0 and run the heavy block`; `FupingJiance() false (helper not running) -> WriteShareMemory(0, [0x01], 1) and return early, skipping game-mode and both config transfers`; `ucThemeLocal1.isGame false -> myCpuHigh=false, myCpuCount=0`; `isGame && myCpuHigh && load <= myCPU && myCpuCount <= 0 -> myCpuHigh=false and return early (skips GetConfigurationToMemory/SetConfigurationToMemory this tick)`; `isGame && myCpuHigh && load <= myCPU && myCpuCount > 0 -> myCpuCount--`; `isGame && myCpuHigh && load > myCPU -> myCpuCount=10`; `isGame && !myCpuHigh && load > myCPU && myCpuCount < 10 -> myCpuCount++`; `isGame && !myCpuHigh && load > myCPU && myCpuCount >= 10 -> myCpuHigh=true`; `isGame && !myCpuHigh && load <= myCPU -> myCpuCount=0`; then unconditionally `GetConfigurationToMemory(2)`; `isMouseDown == -1 && !isMouseDownMB -> SetConfigurationToMemory(1)` (config is not pushed while a drag is in progress); outside the 64-gate, `shanPingTimer >= 0 -> decrement it, and when it crosses below 0 set shanPingCount = -1`.
- `Timer_event` (UCVideoCutF.cs:253) — the control's per-tick pump, driven externally at a 15.625 ms nominal period (that constant is used explicitly at line 281). Two jobs: throttled thumbnail refresh while a handle is being dragged, and preview frame playback. While dragging it counts ticks in `mouseDownCount` and refreshes only every 32 ticks (≈500 ms), calling `GetOneImage` with the dragged handle's timestamp text. During preview it advances `yuLanTimer` and only shows the next frame once `yuLanTimer * 15.625` reaches `yuLanCount * 1000 / yuLanHz` ms; the frame file is `allPicAddr` + a 4-digit zero-padded `yuLanCount` + `.bmp`, drawn at (`xVal`, `yVal`) onto a fresh `wValSub` x `hValSub` bitmap that replaces `formLCDImageCut.myBitmap` (old bitmap disposed) before invalidating both the preview form and this control. Key branches: `isMouseDown1 -> tick counter path, refresh from labelStartTimer text at count>=32, return`; `isMouseDown2 -> same from labelStopTimer text, return`; `neither -> mouseDownCount reset to 0`; `isYulan && elapsed < scheduled frame time -> return (wait)`; `isYulan && frame file exists -> composite, publish to formLCDImageCut, yuLanCount++`; `isYulan && frame file missing -> loop back: yuLanCount=1, yuLanTimer=0`; `!isYulan -> yuLanCount=1, yuLanTimer=0`. Composite/publish failures are swallowed.
- `GetWebBackgroundImageDirectory` (FormLCD.cs:2038) — resolves the **local cache directory** for cloud background assets, orientation-aware. Only one resolution pair exists in this form: `Form1.AddDataFilePath("USBLCD\\Web\\2560720\\")` for landscape and `Form1.AddDataFilePath("USBLCD\\Web\\7202560\\")` for portrait — the folder name is literally `{width}{height}` with no separator. It is the sole path source for the cloud-background tree: `GetFileList` (2064) enumerates it, `CheakWebFile` (2132) loads thumbnails from it, and `ThemeWeb` cmd 16 (2168, 2170, 2172) checks/downloads/plays out of it. Its twin `GetWebBackgroundHttpDirectory` (2051) repeats the identical branch to produce `http://www.czhorde.com/tr/bj2560720/` and `http://www.czhorde.com/tr/bj7202560/`, so the remote and local trees are 1:1 by filename. [COPY-PASTE] — same predicate, different constant pair; the two should be one lookup keyed on (is2560x720, orientation) returning both the local and the remote root. Key branches: `is2560x720 false -> the 2560720 (landscape) folder regardless of directionB`; `is2560x720 && (directionB == 0 || directionB == 180) -> the 2560720 folder`; `is2560x720 && directionB is 90 or 270 -> the 7202560 (portrait) folder`.
- `ThemeWeb` (FormLCD.cs:2144) — command dispatcher for the cloud-*background* browser tab (`ucThemeWeb1`); signature takes an int cmd plus three untyped payload objects, of which only `info` is ever used. Allocates two ArrayLists (thumbnail bitmaps, names) up front. For the catalog commands it delegates to `CheakWebFile` (FormLCD.cs:2070, outside my read range — not asserted) and hands the filled arrays to `ucThemeWeb1.SetThemeWeb`; for the apply command it treats `info` as a bare asset id, appends the `.mp4` extension, ensures the file is present in `GetWebBackgroundImageDirectory()` (FormLCD.cs:2038, outside my range), downloading it from `GetWebBackgroundHttpDirectory()` (FormLCD.cs:2051, outside range) via `DownLoadFile` if absent, then converts it into the working theme with `My_Video_To_Theme` (FormLCD.cs:1509, outside range) using the absolute path of the downloaded file. Around that conversion it stops the render timer (`isToTimer` false), clears `isGifMode`, sleeps exactly 100 ms, snapshots the JPEG quality (`myTempDeviceJpgYSL` ← `myDeviceJpgYSL`), forces `myUISubMode` to 0, calls `buttonMS_Mode()`, and afterwards resets `ImageCount` to 0 and re-enables `isGifMode` + `isToTimer`. The status label `labelZXCG` is set to a busy string, shown, `Application.DoEvents()` pumped, then hidden and reset to the idle string. Key branches: `cmd 0..6 (seven catalog pages) and CheakWebFile returns true -> ucThemeWeb1.SetThemeWeb(images, names)`; `cmd 0..6 and CheakWebFile returns false -> nothing published, both ArrayLists still cleared`; `cmd == 16 -> apply-background path`; `cmd == 16 and "<info>.mp4" already exists under the web background dir -> download skipped, success flag stays true`; `cmd == 16 and DownLoadFile returns false -> whole load skipped, no timer/mode mutation at all`; `Form1.Language == 1 -> label busy text "正在载入动画", idle text "执行中"`; `Form1.Language == 2 -> busy "Loading", idle "執行中"`; `any other language -> busy "Loading", idle "working"`; `any cmd outside {0,1,2,3,4,5,6,16} -> no-op`. [COPY-PASTE] the two three-way language ladders are near-identical bodies differing only in the literal string and belong in a per-language string table.
- `ThemeMask` (FormLCD.cs:2339) — command dispatcher for the cloud *mask* browser tab (`ucThemeMask1`); the tab labelled "cloud themes" in the UI actually ships masks, not themes. Catalog command enumerates the orientation-specific mask root via `CheakMaskFile` (FormLCD.cs:2311) and publishes to `ucThemeMask1.SetThemeMask`. Apply command treats `info` as the mask folder name and does three things: (1) opens `<GetFileListMBDir()><info>\01.png` and re-saves it unchanged as `<GifDirectory>\01.png`, the single working-mask slot, then disposes the bitmap; (2) builds the DC path `<GetFileListMBDir()><info>\config1.dc` and feeds it to `ReadSystemConfiguration` (FormLCD.cs:2585) with the read-my-mode flag **false**, so the mask's DC supplies overlay elements, screenshot rect, mask offsets and direction but is forbidden from overwriting `myBjxs` / `myTpxs` / `myUIMode` / `myMode` — a mask never changes which source mode is active; (3) re-syncs the rotation combo with `ucComboBoxA1.SetUCComboBoxMode(directionB / 90 + 1)` (0/90/180/270 → index 1/2/3/4), calls `buttonMS_Mode()`, forces an immediate metric refresh by setting `InfoCount = 100` before calling `GetSystemInfo()`, and writes the mask folder name into the theme-name textbox `textBoxCMM`. The render timer is stopped (`isToTimer` false) for the whole apply and re-enabled unconditionally at the end. Key branches: `cmd == 0 and CheakMaskFile returns true -> ucThemeMask1.SetThemeMask(images, names)`; `cmd == 0 and false -> nothing published, arrays cleared anyway`; `cmd == 16 -> apply-mask path above`; `any other cmd -> no-op, timer never touched`. Note the mask source directory is orientation-dependent (see findings) so the same mask id resolves to a different folder at 0/180 vs 90/270.
- `ThemeSetting` (FormLCD.cs:2375) [GOD] — the entire settings-panel command surface as one flat integer switch: source-mode selection, screenshot geometry, mask control, the eyedropper overlay, the flash/blink timer and four sub-dialog launches all live in one method with an untyped `info` that is variously cast to bool and int. Stops the render timer on entry and restores it with a two-branch rule at the exit. Key branches: `cmd == 1 (background image) -> myUIMode=1, myMode=0, myBjxs=info, myTpxs=false, mySpxs=false, buttonMS_Mode(), myTempDeviceJpgYSL=myDeviceJpgYSL`; `cmd == 2 (screen mirror) -> myUIMode=2, myMode=16, myTpxs=info, myBjxs=false, mySpxs=false, same tail`; `cmd == 3 (video player) -> myUIMode=4, myMode=48, mySpxs=info, myTpxs=false, myBjxs=false, same tail (contains an empty if-mySpxs body — dead)`; `cmd == 10 -> VideoSelect_Open_Generate()`; `cmd == 49 -> ImageCut_Open()`; `cmd == 51 -> buttonYDZT_Click(null, null)`; `cmd == 52 -> VideoCut_Open()`; `cmd == 65 -> JpX = info`; `cmd == 66 -> JpY = info`; `cmd == 67 -> JpW = info`; `cmd == 68 -> JpH = info`; `cmd == 69 and info true -> myYcbk=true and formJP hidden`; `cmd == 69 and info false -> myYcbk=false and buttonSelectBackgroundImage()`; `cmd == 96 -> myMbxs = info (mask visibility)`; `cmd == 97 -> MengBanSelect_Open() (pick a local PNG mask)`; `cmd == 99 -> buttonYDMB_Click(null, null) (cloud mask tab)`; `cmd == 112 -> formGetColor shown at Top=0 Left=0 sized to Screen.PrimaryScreen.Bounds.Width/Height (full-screen eyedropper capture surface)`; `cmd == 128 -> myXtxx = info (system-info overlay on/off)`; `cmd == 129 -> shanPingCount = info, shanPingTimer = 14, SetConfigurationToMemory(1)`; `any other cmd -> no state change`. Exit rule: `myUIMode != 1 -> isToTimer = true`; `myUIMode == 1 and myUISubMode != 2 -> isToTimer = true`; `myUIMode == 1 and myUISubMode == 2 -> isToTimer stays false`, i.e. the still-image sub-mode deliberately leaves the render loop stopped. Note `myMode` literals 0 / 16 / 48 are the image / screenshot / video source selectors and pair 1:1 with `myUIMode` 1 / 2 / 4 — two fields carrying one fact, which a port should collapse to a single enum.

## The authoring controls for this device class

Three more files arrived with `TRCC.LCD` and serve only it. `UCImageCutF.cs` and
`UCVideoCutF.cs` are the `F` siblings of the `UCImageCut` / `UCVideoCut` crop
controls documented in [`AUDIT_THEME_MASK.md`](AUDIT_THEME_MASK.md) and
[`AUDIT_VIDEO.md`](AUDIT_VIDEO.md) — same job, sized for the ultrawide strip
rather than a square or widescreen panel. `FormLCDImageCut.cs` is the frameless
crop overlay this host positions over the physical monitor (see the
`myX/myY/myW/myH` placement in `FormCZTVInit`).

Where an `F` control and its non-`F` original agree, the original's audit still
applies and is not restated; where they differ, the difference is stated here.

- `UCVideoCutF` (UCVideoCutF.cs:179) — constructor for the 2560x720 ("F") video-trim control. Clears `buttonTPJCOK`'s flat border to fully transparent ARGB(0,0,0,0); loads the three timeline sprites from resources: `P剪辑块a` into `bitmap1` (left/start handle, centre initialised `bitmapCX1`=16, `bitmapCY1`=43), `P剪辑块b` into `bitmap2` (right/stop handle, centre `bitmapCX2`=495, `bitmapCY2`=56), and `P进度条` into `bitmapJindu` (progress bar origin `bitmapJDX`=16, `bitmapJDY`=45); each sprite's own Width/Height are cached into `bitmapW1/H1`, `bitmapW2/H2`, `bitmapJDW/JDH`. Paths: `onePic` = data-dir + `Temp\0.png`, `allPicAddr` = data-dir + `Temp\`, both via `Form1.AddDataFilePath`. Instantiates `formLCDImageCut` (the live preview window). Key branches: `Form1.Language == 1 -> labelInfo text "正在渲染"`; `Form1.Language == 2 -> "Rendering"`; `otherwise -> "Rendering"` (branches 2 and 3 are identical). EXTERNAL: Form1.cs (`AddDataFilePath`, `Language`), FormLCDImageCut.cs.
- `OnPaint` (FormLCDImageCut.cs:26) — Chains to the base `Form.OnPaint`, then blits the field `myBitmap` at (0,0) with no scaling, no clipping and no size negotiation — the bitmap is expected to already match the 2560x720 client area. Key branches: `DrawImage throws (e.g. myBitmap still null) -> swallowed by an empty catch, the form simply paints its black background` (30-36).
- `KillFFmpegYulan` (UCVideoCutF.cs:228) — stops preview playback: clears `isYulan` and restores `buttonYulan`'s background to resource `P0预览` (the idle preview icon). Optionally terminates every running process whose `ProcessName` equals `"ffmpeg"`, enumerated over all system processes; all exceptions swallowed. Key branches: `kill == false (default is true) -> return before enumerating processes, leaving ffmpeg alive`; `kill == true -> kill every ffmpeg process`.
- `buttonClose_Click` (UCImageCutF.cs:343) — cancel button: invokes the `ucImageCut` callback with a null image (:345), which is how the host distinguishes cancel from confirm. Key branches: `ucImageCut` null `-> ` nothing is dispatched.
- `buttonTPJCH_Click` (UCImageCutF.cs:279) [COPY-PASTE] — "fit height" button, the mirror of `buttonTPJCW_Click`. Sets `isTPJCW`=false (:287), disposes both sub-images (:288-296), recomputes the fit so the image spans the full panel height with the width derived from the source aspect and horizontally centred, re-renders `imageSub0` → `imageSub` (:318-324), resets `bili`/`oldBili`=1.0 and `bitmapCX`=254 (:325-327), republishes `imageSub` to the preview window (:328-333) and invalidates both surfaces (:334-335). Note the ordering differs cosmetically from the W variant (zoom reset before the preview swap) but the effect is identical. Key branches: `is2560x720 && isFanZhuan -> imageSub` 720×2560, `hVal`=2560, `wVal`=`imageAll`.Width×2560/`imageAll`.Height, `xVal`=(720−`wVal`)/2 (:301-306); `is2560x720 && !isFanZhuan -> imageSub` 2560×720, `hVal`=720, `wVal`=Width×720/Height, `xVal`=(2560−`wVal`)/2 (:310-315); `!is2560x720 -> ` no buffer allocated.
- `buttonTPJCW_Click` (UCImageCutF.cs:220) [COPY-PASTE] — "fit width" button. Sets `isTPJCW`=true (:228), disposes `imageSub0`/`imageSub` (:229-237), recomputes the fit so the image spans the full panel width with the height derived from the source aspect and vertically centred, re-renders the same two-stage `imageSub0` → `imageSub` composite (:259-265), republishes `imageSub` to the preview window's `myBitmap` (:266-271), resets `bili`/`oldBili` to 1.0 and the knob `bitmapCX` to 254 (:272-274), and invalidates both this control and the preview window (:275-276). Near-duplicate of `buttonTPJCH_Click`. Key branches: `is2560x720 && isFanZhuan -> imageSub` 720×2560, `wVal`=720, `hVal`=`imageAll`.Height×720/`imageAll`.Width, `yVal`=(2560−`hVal`)/2 (:242-247); `is2560x720 && !isFanZhuan -> imageSub` 2560×720, `wVal`=2560, `hVal`=Height×2560/Width, `yVal`=(720−`hVal`)/2 (:251-256); `!is2560x720 -> ` no buffer allocated (same unusable path as `SetImage`).
- `buttonXuanzhuan_Click` (UCVideoCutF.cs:443) — rotate control. Advances `bitAngle` by +270 modulo 360 (equivalently −90 per press, i.e. counter-clockwise stepping through 0 -> 270 -> 180 -> 90 -> 0) and recomputes the rotation-adjusted source dimensions from `originalImageW`/`originalImageH`, then re-applies the current fit mode. `bitAngle` is later handed to ffmpeg as `-display_rotation`. Key branches: `isZhuanma -> do nothing`; `bitAngle == 0 -> bitAngleW=originalImageW, bitAngleH=originalImageH`; `bitAngle == 90 -> bitAngleW=originalImageH, bitAngleH=originalImageW (swapped)`; `bitAngle == 180 -> unswapped, same as 0`; `bitAngle == 270 -> swapped, same as 90`; `isTPJCW -> call buttonTPJCW_Click`; `!isTPJCW -> call buttonTPJCH_Click`. Kills preview first.
- `ResetAllTempFile` (UCVideoCutF.cs:479) — wipes the frame scratch directory `allPicAddr` (data-dir `Temp\`) recursively and recreates it empty. Key branches: `directory exists -> recursive delete, then create`; `directory missing -> create only`.
- `BitmapToByte` (UCVideoCutF.cs:488) — encodes a bitmap to JPEG bytes in a MemoryStream and returns the stream's internal buffer via `GetBuffer` (note: this is the whole capacity-sized buffer, not a length-trimmed copy; callers write `array.Length` as the frame size). Retries forever on failure. Key branches: `encode throws -> sleep 100 ms, pump the message loop, retry`; `encode succeeds -> return buffer`.
- `BmpToThemeFile` (UCVideoCutF.cs:513) — builds `Theme.zt` in `allPicAddr` from the numbered BMP frames, WITHOUT compositing them onto the sub-canvas (frames are written at whatever size ffmpeg produced). Container layout written in order: one magic byte 220 (0xDC); then a 32-bit frame count (`num`−1); then that many 32-bit millisecond timestamps computed as index * 41.666666666666664 (a hardcoded 24 fps step, ignoring `originalImageHz`); then per frame a 32-bit byte length followed by the JPEG bytes. Frames are consumed in ascending 4-digit zero-padded order starting at 0001, each deleted after being read. The collection loop is a miss-counter: it exits once 100 consecutive misses (1 ms sleep each) occur, i.e. ~100 ms of no new frame. Opened with FileMode.OpenOrCreate (no truncate). [COPY-PASTE] the writer half is a near-duplicate of `ZhuanMaPanDuan` (:570). Key branches: `frame file exists -> encode, append, delete, reset miss counter, advance index`; `read/encode throws -> sleep 1 ms and retry the same index`; `frame file missing -> miss counter++, sleep 1 ms`; `miss counter reaches 100 -> stop and write the header/timestamps/payload`. No call site for this method appears within lines 179-1535.
- `ZhuanMaPanDuan` (UCVideoCutF.cs:570) — [GOD] the encode driver: optionally re-runs ffmpeg for the selected range, then composites and packs every frame into `Theme.zt`. When re-running, it first clamps the clip to 300000 ms (5 minutes) by pulling `stopTimerVal` back, resyncing `bitmapCX2` and `labelStopTimer`, and repainting; then `ResetAllTempFile()`; then spawns `cmd.exe` (UseShellExecute false, redirected stdin/stdout, no window), does `cd /d` to `Application.StartupPath`, and issues one ffmpeg command whose argument order is: `-display_rotation` `bitAngle`, `-ss` start (hh:mm:ss.mmm), `-t` duration (hh:mm:ss.mmm), `-i` the source path `myName`, `-y`, `-r` `originalImageHz`, `-s` `wVal`x`hVal`, `-f image2`, output `allPicAddr` + `%04d.bmp`; sleeps 100 ms and closes the process (it does NOT wait for exit — frames are consumed while ffmpeg still writes). Packing: magic byte 220 (0xDC), 32-bit frame count (`num`−1), that many 32-bit millisecond timestamps stepped by 1000/`originalImageHz`, then per frame a 32-bit length and the JPEG bytes. Each BMP is drawn at (`xVal`, `yVal`) onto a fresh `wValSub` x `hValSub` bitmap before JPEG encoding, then deleted. Progress: expected frame count is `(stopTimerVal-startTimerVal)/1000 * originalImageHz`; `jindu` = index/expected * `bitmapJDW`, clamped to `bitmapJDW`, and the control repaints only when `jindu` crosses the next 24-pixel threshold (initial threshold 24). `jindu` is reset to 0 at the end. Key branches: `isFF == false -> skip ResetAllTempFile and the ffmpeg spawn entirely, pack the frames the running preview ffmpeg is already producing`; `isFF == true && stopTimerVal-startTimerVal > 300000 -> clamp stop to start+300000 and resync the handle/label`; `frame file exists -> composite, encode, append, delete, advance, miss counter reset`; `composite/encode throws -> sleep 1 ms, retry same index`; `frame missing -> miss counter++, sleep 1 ms`; `miss counter reaches 100 -> stop`; `computed progress > bitmapJDW -> clamp to bitmapJDW`; `jindu >= next threshold -> repaint and raise threshold by 24`. `Theme.zt` opened FileMode.OpenOrCreate (no truncate).
- `buttonTPJCOK_Click` (UCImageCutF.cs:338) — confirm/crop button: invokes the `ucImageCut` callback delegate with the current panel-sized composite `imageSub` as the result image (:340). The delivered bitmap is exactly the panel buffer (2560×720, or 720×2560 when `isFanZhuan`), already letterboxed/panned/zoomed — no further cropping happens here. Key branches: `ucImageCut` null `-> ` nothing is dispatched (null-conditional invoke). Porting hazard: the same object stays referenced by `this.imageSub` and by the preview window's `myBitmap`, so the receiver does not get an owned copy.
- `buttonYulan_Click` (UCVideoCutF.cs:702) — starts preview playback. Kills any prior ffmpeg, wipes the temp frame directory, then spawns `cmd.exe` (UseShellExecute false, redirected stdin/stdout, no window), does `cd /d` to `Application.StartupPath`, and issues the same ffmpeg extraction line used by the encoder: `-display_rotation` `bitAngle`, `-ss` start, `-t` duration, `-i` `myName`, `-y`, `-r` `originalImageHz`, `-s` `wVal`x`hVal`, `-f image2`, `allPicAddr` + `%04d.bmp`; the process is closed immediately without waiting, so `Timer_event` plays frames as they land. Sets `yuLanHz` = `originalImageHz` (the playback pacing rate), `isYulan` = true, and swaps `buttonYulan`'s background to resource `P0预览a` (the active-preview icon). Key branches: `isZhuanma || isYulan -> do nothing`; `otherwise -> start preview`.
- `UCVideoCut_MouseTimer` (UCVideoCutF.cs:737) — converts a timeline pixel x to a millisecond position: (x − 16) * (`originalTimerLen` / 479), where 16 is the track's left pixel and 479 the usable track width in pixels (16..495 inclusive). No branches.
- `UCVideoCut_TimerMouse` (UCVideoCutF.cs:744) — inverse of `UCVideoCut_MouseTimer`: converts a millisecond position to a timeline pixel x as round(ms / `originalTimerLen` * 479 + 16, 0). No branches.
- `UCVideoCut_TimerToLong` (UCVideoCutF.cs:750) — parses an `H:MM:SS.fff` string (colon-split; the seconds field is split on the first '.') into milliseconds. Fractional part is scaled by 10^(−digit count) so it tolerates 1-, 2- or 3-digit fractions; hours*3600 + minutes*60 + seconds are summed, rounded to 2 decimal places, then multiplied by 1000. No branches (malformed input throws, and callers catch).
- `UCVideoCut_LongToTimer` (UCVideoCutF.cs:761) — formats milliseconds as `HH:MM:SS.fff` with 2-digit hours/minutes/seconds and 3-digit milliseconds; hours are not wrapped at 24. This is the exact string handed to ffmpeg's `-ss` and `-t`. No branches.
- `GetOneImage` (UCVideoCutF.cs:778) — extracts one still at the given `H:MM:SS.fff` timestamp and publishes it to the preview window. Deletes `onePic` (`Temp\0.png`), spawns `cmd.exe` (UseShellExecute false, redirected stdin/stdout, no window), `cd /d` to `Application.StartupPath`, and issues ffmpeg with argument order `-display_rotation` `bitAngle`, `-ss` timestamp, `-i` `myName`, `-y`, `-s` `wVal`x`hVal`, `-frames:v 1`, `-f image2`, output `onePic`. Then polls for the file's existence at 1 ms intervals. On success the PNG is drawn at (`xVal`, `yVal`) onto a new `wValSub` x `hValSub` bitmap which replaces `formLCDImageCut.myBitmap` (previous bitmap disposed), and both the preview form and this control are invalidated. Key branches: `isGetOneImage already set -> return (re-entrancy guard)`; `requested ms + 1000 > originalTimerLen -> seek clamped to originalTimerLen − 1000 and the timestamp string regenerated`; `file still absent after 100 polls (~100 ms) -> clear isGetOneImage and return without updating the preview (the cmd process is left unclosed)`; `file appears -> composite and publish`.
- `UCVideoCut_MouseDown` (UCVideoCutF.cs:840) — begins a handle drag. Hit-testing: the vertical band is y 23..106; x is clamped to 16..495; a handle is grabbed when both |x − handle centre x| < 15 and |y − handle centre y| < 15. Grabbing a handle kills preview, moves that handle to the clamped x, converts the pixel to a time via `UCVideoCut_MouseTimer`, updates `labelStartTimer`/`labelStopTimer`, enforces the 5-minute (300000 ms) maximum span by pushing the OPPOSITE handle, refreshes `labelTimer` with the span, and invalidates. Key branches: `isZhuanma -> return`; `e.Y < 23 || e.Y > 106 -> return`; `x < 16 -> clamp to 16`; `x > 495 -> clamp to 495`; `start handle hit -> isMouseDown1 set; if bitmapCX1 >= bitmapCX2 then bitmapCX1 = bitmapCX2 − 1; if span > 300000 then stopTimerVal = startTimerVal + 300000 and bitmapCX2/label resynced`; `else stop handle hit -> isMouseDown2 set; if bitmapCX1 >= bitmapCX2 then bitmapCX2 = bitmapCX1 + 1; if span > 300000 then startTimerVal = stopTimerVal − 300000 and bitmapCX1/label resynced`; `neither hit -> nothing beyond clearing isMouseMove and recording xPos/yPos`.
- `UCVideoCut_MouseMove` (UCVideoCutF.cs:903) — drags the grabbed handle. `isMouseMove` is used as a non-reentrancy latch, set on entry and cleared at the end of the body, so a move event arriving while one is being processed is dropped. Same clamping (x to 16..495), same anti-crossing rule (handles kept at least 1 px apart), same 300000 ms span cap pushing the opposite handle, same label/`labelTimer` updates and invalidate as `UCVideoCut_MouseDown`. [COPY-PASTE] the two branch bodies duplicate each other and duplicate the corresponding bodies in `UCVideoCut_MouseDown` (:840). Key branches: `isMouseDown1 && !isMouseMove -> move start handle (bitmapCX1 = clamped x; if bitmapCX1 >= bitmapCX2 then bitmapCX2 − 1; if span > 300000 then stop pulled to start+300000)`; `isMouseDown2 && !isMouseMove -> move stop handle (bitmapCX2 = clamped x; if bitmapCX1 >= bitmapCX2 then bitmapCX1 + 1; if span > 300000 then start pushed to stop−300000)`; `isMouseMove already true, or no handle grabbed -> ignore the event`. Note there is no `isZhuanma` guard here.
- `UCVideoCut_MouseUp` (UCVideoCutF.cs:968) — ends the drag and refreshes the still preview at the handle just released. Sets which handle is "current" (`isMouse1`/`isMouse2`, the flag the fit/rotate buttons later consult), wipes the temp frame directory, and pulls one frame. Clears `isMouseDown1`, `isMouseDown2`, `isMouseMove` unconditionally. Key branches: `isMouseDown1 -> isMouse1=true, isMouse2=false, ResetAllTempFile(), GetOneImage(labelStartTimer text)`; `isMouseDown2 -> isMouse1=false, isMouse2=true, ResetAllTempFile(), GetOneImage(labelStopTimer text)` (both are tested, not else-if); `neither -> flags cleared only`.
- `SetImage` (UCImageCutF.cs:128) — loads a new source image and computes the initial letterbox fit. Swaps `imageAll` to the incoming image and disposes the previous one plus any existing `imageSub0`/`imageSub` (:136-150). Builds the destination buffer and fit geometry, then renders in two stages: `imageSub0` is a bitmap of `wVal`×`hVal` holding the source scaled to the fit size (:194-197), and that is blitted at offset (`xVal`,`yVal`) into `imageSub`, the panel-sized buffer (:198-200). Hands `imageSub` to the preview window's `myBitmap`, disposing the old one, and invalidates it (:201-207). Resets the zoom state to `bili`=1.0, `oldBili`=1.0, knob `bitmapCX`=254 (:208-210). Key branches: `is2560x720 && isFanZhuan -> imageSub` is 720×2560 portrait (:155) and the fit test compares source width against height×9/32 (:158): wider than that ratio `-> wVal`=720, `hVal`=height×720/width, `yVal` centred by (2560−`hVal`)/2, `isTPJCW`=true (:160-163); otherwise `-> hVal`=2560, `wVal`=width×2560/height, `xVal` centred by (720−`wVal`)/2, `isTPJCW`=false (:167-170). `is2560x720 && !isFanZhuan -> imageSub` is 2560×720 landscape (:175) and the test compares source height against width×9/32 (:178): taller `-> hVal`=720, `wVal`=width×720/height, `xVal` centred by (2560−`wVal`)/2, `isTPJCW`=false (:180-183); otherwise `-> wVal`=2560, `hVal`=height×2560/width, `yVal` centred by (720−`hVal`)/2, `isTPJCW`=true (:187-190). `!is2560x720 -> ` no buffer is allocated at all, and since `imageSub` was just nulled at :146-150 the draw at :198 has no target — the control is only usable with `is2560x720` set. The constant 9/32 = 0.28125 is exactly 720/2560, i.e. the panel aspect.
- `GetVideoRatio` (UCVideoCutF.cs:1288) — scans ffmpeg's `Video:` line character-by-character for the first `<digits>x<digits>` token and returns it as a char array. Tries four fixed shapes in priority order and returns on the first hit, so wider forms win over narrower ones. Key branches: `4 digits + 'x' + 4 digits found -> return that 9-char run (e.g. 1920x1080)`; `else 4 digits + 'x' + 3 digits -> return that 8-char run (e.g. 1280x720)`; `else 3 digits + 'x' + 4 digits -> return that 8-char run (e.g. 720x1280)`; `else 3 digits + 'x' + 3 digits -> return that 7-char run (e.g. 640x480)`; `no match -> return null`. Note none of the loops match 2-digit or 5-digit components, and each of the last three loops bounds at length−7.
- `button1_Click` (UCImageCutF.cs:385) — rotate button: rotates `imageAll` by +90° via `RotateImg`, disposes the old source and replaces it with the rotated one (:387-390), then re-runs the current fit so the new aspect is re-letterboxed and the zoom/pan state is reset. Key branches: `isTPJCW -> buttonTPJCW_Click` is called with null args (:393); otherwise `-> buttonTPJCH_Click` is called with null args (:397). Each click accumulates another 90° on the already-rotated source, so four clicks return to the original orientation (with resampling loss).
- `button2_Click` (UCVideoCutF.cs:1369) — frame-rate selector: sets `originalImageHz` = 24 and swaps the radio artwork (`button2` selected `P点选框A`, `button1` unselected `P点选框`); timestamp step becomes 1000/24 = 41.667 ms. [COPY-PASTE] mirror of `button1_Click` (:1362). No branches.
- `Dispose` (FormLCDImageCut.cs:44) — Standard designer dispose: disposes the `components` container then chains to `Form.Dispose`. It does NOT dispose `myBitmap`. Key branches: `disposing true and components non-null -> components disposed`; otherwise only the base call runs (46-50).
- `InitializeComponent` (FormLCDImageCut.cs:53) — Designer plumbing: a borderless (FormBorderStyle 0 = None), TopMost, DoubleBuffered form with black BackColor, BackgroundImageLayout 0 = None, AutoScaleMode 3 = Inherit, ClientSize 2560x720, Name/Text "FormLCDImageCut", icon pulled from the `ComponentResourceManager` key "$this.Icon", and the single `FormClosing` handler wired. No child controls are created.
- `UCImageCutF` (UCImageCutF.cs:83) — constructor for the 2560×720 image-crop control. Zeroes the image handles (`imageAll`, `imageSub0`, `imageSub`, `bitmap`) and the drag/zoom state, then seeds the working geometry to the panel default: `xVal`=0, `yVal`=0, `wVal`=2560, `hVal`=720 (:103-106), `bili`=1.0 and `oldBili`=1.0 (:107-108), `isTPJCW`=true (fit-width mode, :112), and the flags `isMouseDownGdt`/`isFanZhuan`/`is2560x720`/`isBiliPingmu`/`isMouseDown` all false (:99-102, :109). Runs `InitializeComponent` (:116), loads the zoom-slider knob from resource `P滑动条按钮` into `bitmap` and caches its `bitmapW`/`bitmapH` (:117-119), and parks the knob at `bitmapCX`=254, `bitmapCY`=50 (:120-121) — 254 is the unity-scale detent used by both scaling routines. Creates the separate preview window `FormLCDImageCut` (:122, EXTERNAL: FormLCDImageCut.cs) and subscribes its MouseDown/MouseMove/MouseUp to this control's own three handlers (:123-125), so the preview window and the slider strip share one handler set and are told apart by the `sender` identity check. No branches.
- `RotateImg` (UCImageCutF.cs:348) — generic rotation helper. Takes an image and an angle in degrees, rotates about the image centre and returns a NEW bitmap sized to the rotated bounding box, so no content is clipped. Computes the bounds by rotating a rectangle of the source size with a matrix using MatrixOrder value 1 (Append) about the centre point (:358-362), rounds the bounds X/Y/Width/Height to whole pixels (:363-366), allocates the destination at those rounded dimensions (:367), sets InterpolationMode value 3 (Bilinear) and SmoothingMode value 2 (HighQuality) (:369-370), centres the source rectangle inside the new bounds (:371-372), then translate-to-centre / rotate / translate-back and draws the source into that rectangle (:373-377). Resets the transform, saves and disposes the graphics and path, returns the new bitmap (:378-382). No branches.
- `ImageBiliBianhuan` (UCImageCutF.cs:401) [COPY-PASTE] — live (during-drag) zoom preview. Maps the slider knob position `bitmapCX` to the scale `bili`, computes the new scaled size from `wVal`×`hVal`, keeps the image centred by shifting `xVal`/`yVal` by half the size delta against the previous scale `oldBili` (:415-421), then rebuilds only the panel buffer `imageSub` and stretches the existing `imageSub0` into it at the new size (:438-439) — the fit-sized `imageSub0` is NOT re-rendered from `imageAll`, so this pass is fast but resamples an already-resampled bitmap. Publishes the buffer to the preview window's `myBitmap`, disposes the old one and invalidates it (:441-447). Key branches: `bitmapCX` > 254 `-> bili` = 1 + (`bitmapCX`−254)×0.03 (:409, max 8.08 at the 490 clamp); otherwise `-> bili` = 1 / (1 + (254−`bitmapCX`)×0.03) (:413, min ≈0.1238 at the 18 clamp). `is2560x720 && isFanZhuan -> imageSub` 720×2560 (:431); `is2560x720 && !isFanZhuan -> imageSub` 2560×720 (:435); `!is2560x720 -> ` no buffer allocated.
- `ImageBiliBianhuanUp` (UCImageCutF.cs:450) [COPY-PASTE] — quality re-render committed on mouse-up. Recomputes `bili` from `bitmapCX` and the centring shift with the identical formulas as `ImageBiliBianhuan` (:458-472); because `oldBili` was already advanced by the last live pass, the size delta is normally zero and the offsets do not move. Differs in that it also disposes and REBUILDS `imageSub0` at the zoomed size directly from `imageAll` (:473-495) before blitting it into `imageSub` at (`xVal`,`yVal`) (:497-498), so the committed image is resampled once from the original instead of twice. Publishes to the preview window's `myBitmap` and invalidates (:500-506). Key branches: `bitmapCX` > 254 `-> bili` = 1 + (`bitmapCX`−254)×0.03 (:460); otherwise `-> bili` = 1 / (1 + (254−`bitmapCX`)×0.03) (:464). `is2560x720 && isFanZhuan -> imageSub` 720×2560 (:486); `is2560x720 && !isFanZhuan -> imageSub` 2560×720 (:490); `!is2560x720 -> ` no buffer allocated.
- `UCImageCut_MouseDown` (UCImageCutF.cs:509) — one handler serving two surfaces, dispatched on `sender` identity. Key branches: `sender` is `formLCDImageCut` `-> ` starts an image pan: `isMouseDown`=true and the anchor `xPos`/`yPos` is set to the event point, then returns (:511-517). Any other sender (this control, i.e. the slider strip) `-> ` starts a slider drag: `isMouseDownGdt`=true, `bitmapCX` is set to the event X and clamped to the track range 18…490 (:518-527), then `ImageBiliBianhuan` runs and the control repaints (:528-529). The 18/490 clamp with the 254 unity detent means the track is asymmetric: 236 px of travel on each side of unity.
- `UCImageCut_MouseMove` (UCImageCutF.cs:532) — handles both an in-progress pan and an in-progress slider drag; the two blocks are independent, not exclusive. Key branches: `isMouseDown -> ` pan: `xVal`/`yVal` accumulate the delta from the last `xPos`/`yPos`, the anchor advances (:540-543), `imageSub` is disposed and reallocated at the panel size, `imageSub0` is blitted into it at the new offset (:544-562), the buffer is republished to the preview window's `myBitmap` (:563-568) and both surfaces are invalidated (:569-570). Inside that block: `is2560x720 && isFanZhuan -> imageSub` 720×2560 (:553); `is2560x720 && !isFanZhuan -> imageSub` 2560×720 (:557); `!is2560x720 -> ` no buffer allocated. `isMouseDownGdt -> ` slider drag: `bitmapCX` tracks the event X clamped to 18…490, `ImageBiliBianhuan` runs and the control repaints (:574-584). Panning is unbounded — nothing clamps `xVal`/`yVal` to keep content over the panel.
- `UCImageCut_MouseUp` (UCImageCutF.cs:588) — ends both gestures. Key branches: `isMouseDownGdt -> ImageBiliBianhuanUp` runs the quality re-render from `imageAll` and the control repaints (:590-593). Unconditionally clears `isMouseDown` and `isMouseDownGdt` (:595-596); a pan therefore commits nothing extra, since every move already rendered the final buffer.
- `FormLCDImageCut` (FormLCDImageCut.cs:21) — Constructor; calls `InitializeComponent` and nothing else. The bitmap the form paints (`myBitmap`) is not assigned here, so it must be set by the caller after construction. No branches.
- `FormLCDImageCut_FormClosing` (FormLCDImageCut.cs:39) — Unconditionally cancels every close attempt by setting `Cancel` on the event args, for any close reason including the user and the shell. The form can therefore only be torn down by disposing it directly from code. No branches.
